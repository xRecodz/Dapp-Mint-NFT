{
  "name": "dapp-minting-page-nft",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.26.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@pinata/sdk": "^2.1.0",
    "dotenv": "^16.0.3",
    "ethers": "^6.15.0"
  }
}
